"use client";

import { createTheme } from "@mui/material/styles";

// Bare Material baseline — no custom branding yet (per spec: minimal design first).
const theme = createTheme({
  palette: { mode: "light" },
});

export default theme;
