import StubPage from "@/components/StubPage";

// Rarities (collectible tokens) — not shipped yet; placeholder "soon" page linked
// from the Bank's rarities tile.
export default function RaritiesPage() {
  return <StubPage titleKey="bank.rarities" />;
}
